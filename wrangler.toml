# Cloudflare Workers — static-assets deployment
# Build command:   npm run build
# Deploy command:  npx wrangler deploy
name = "creatorgig"
compatibility_date = "2026-05-29"

# Serve the static build output as Worker assets (no server-side code).
[assets]
directory = "./dist"
